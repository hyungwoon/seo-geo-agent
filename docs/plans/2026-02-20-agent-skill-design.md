# SEO/GEO Rewriter Agent/Skill 구조 설계

Date: 2026-02-20

## 목표

사용자가 아티클(플레인 텍스트)을 붙여넣으면 Claude가 SEO/GEO 규칙에 따라 최적화하여 결과물을 채팅에 직접 출력하는 대화형 워크플로우.

## 핵심 결정사항

- **최적화 엔진**: Claude LLM 직접 (Node.js 코드 미사용)
- **입력 형식**: 플레인 텍스트
- **출력**: 채팅에 직접 (제목, Post URL, 메타 태그, Excerpt, 본문 HTML)
- **아키텍처**: Skill + Sub-agent 파이프라인

## 파일 구조

```
seo-geo-agent/                          # 현재 프로젝트 (기존 파일 유지)
├── .claude/
│   └── agents/
│       ├── seo-analyzer.md             # 분석 전문 sub-agent (haiku)
│       └── seo-writer.md               # 리라이팅 전문 sub-agent (sonnet)
│
├── config/
│   └── searchlight-content-strategy.md # 기존 유지 (규칙 소스)
│
├── src/                                # 기존 CLI 코드 유지
├── tests/                              # 기존 테스트 유지
└── CLAUDE.md                           # 새 구조 반영 업데이트

~/.claude/skills/seo-geo-rewriter/      # 사용자 레벨 Skill
├── SKILL.md                            # 오케스트레이션 + 워크플로우
└── rules/
    └── searchlight-strategy.md         # 핵심 규칙 요약본
```

## 워크플로우

```
사용자: /seo-geo-rewriter [키워드: 선택사항]
       + 아티클 텍스트 붙여넣기
            ↓
SKILL.md (오케스트레이터)
  - 키워드 없으면 자동 추출 지시
  - config/searchlight-content-strategy.md 동적 주입
            ↓
[1] seo-analyzer sub-agent (haiku, read-only)
    - SEO 점수 계산 (0-100)
    - GEO citability 평가 (0-100)
    - 이슈 목록 (critical/high/medium/low)
    - 키워드 배치 분석
    - Searchlight 브랜드 규칙 체크
    - 분석 결과 JSON 반환
            ↓
[2] seo-writer sub-agent (sonnet)
    - 분석 결과 기반 최적화
    - 제목 (H1, ≤28자 권장)
    - Post URL / slug 생성
    - 메타 설명 (120-160자)
    - Excerpt (2-3문장)
    - 본문 HTML (전체 리라이팅)
            ↓
결과물 채팅 출력:
  📌 제목 | 🔗 slug | 📝 메타 | 💬 Excerpt | 📄 본문 | 📊 스코어
```

## Sub-agent 사양

### seo-analyzer.md
- **모델**: haiku (빠름, 저비용)
- **도구**: Read (파일 읽기 전용)
- **역할**: 분석 + 점수 + 이슈 목록
- **출력 형식**: 구조화된 마크다운 (JSON-like)

### seo-writer.md
- **모델**: sonnet
- **도구**: 없음 (순수 텍스트 생성)
- **역할**: 분석 결과 기반 완전 리라이팅
- **Searchlight 규칙**: searchlight-content-strategy.md 참조

## Skill 사양

### SKILL.md
- **위치**: `~/.claude/skills/seo-geo-rewriter/`
- **트리거**: `/seo-geo-rewriter` 또는 SEO 최적화 요청 시 자동
- **context**: fork (격리된 sub-agent 컨텍스트)
- **agent**: general-purpose (두 sub-agent 오케스트레이션)
- **동적 주입**: `!cat config/searchlight-content-strategy.md`

## 기존 코드 처리

- `src/` — 유지 (CLI 배치 처리 병렬 워크플로우)
- `tests/` — 유지
- `config/searchlight-content-strategy.md` — 유지 + Skill에서 참조
- `CLAUDE.md` — 업데이트 (새 에이전트 구조 반영)

## 성공 기준

1. `/seo-geo-rewriter` + 텍스트 붙여넣기로 즉시 실행
2. Searchlight 브랜드 규칙 자동 적용
3. 결과물 5개 항목 채팅에 직접 출력
4. 키워드 미입력 시 자동 추출
